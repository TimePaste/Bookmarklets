javascript:(function() {    var collapsibleMenus = document.querySelectorAll('a.btn.btn-sm.collapsed');    collapsibleMenus.forEach(function(menu) {        if (menu.getAttribute('aria-expanded') === 'false') {            menu.click();        }    });    var dropdowns = document.querySelectorAll('div.ms-drop ul');        dropdowns.forEach(function(dropdown) {        var checkboxes = dropdown.querySelectorAll('input[type="checkbox"]');                checkboxes.forEach(function(checkbox) {            var labelText = checkbox.nextElementSibling.textContent.trim();            if (labelText === "newmailbox@otherdomain.com" && !checkbox.checked) {                checkbox.click();            }                        if (labelText === "oldmailbox@gmail.com" && checkbox.checked) {                checkbox.click();            }        });    });})();
